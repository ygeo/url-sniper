# docs/txt/url-sniper/build-docker-image/build-docker-image.spec.txt

	doSpecBuildDockerImage
	
	# Purpose:
	# ------------------------------------------------------
	to build a docker image from a Dockerfile for specific project

	# Prequisites
	# ------------------------------------------------------
	Installed and configured docker-engine
   , Dockerfile under sfw/docker/proj/ dir


	# Interdependancies
	# ------------------------------------------------------
   
	# Usage:
	# ------------------------------------------------------
   this action should be used as follows:
   bash sfw/bash/url-sniper/url-sniper.sh -a build-docker-image


	# Developent
	# ------------------------------------------------------
	-- todo: add a simple Dockerfile under proj
   -- todo: parametrize the proj into -p $proj

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doSpecBuildDockerImage functions



# eof file: docs/txt/url-sniper/build-docker-image/build-docker-image.spec.txt
