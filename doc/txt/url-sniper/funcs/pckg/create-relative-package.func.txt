# doc/txt/url-sniper/create-relative-package/create-relative-package.func.txt

	doCreateRelativePackage
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doCreateRelativePackage function

	# Prequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doCreateRelativePackage function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doCreateRelativePackage function to execute without errors

	# Interdependancies
	# ------------------------------------------------------
	-- todo: what are the interdependancies for the  
		doCreateRelativePackage function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doCreateRelativePackage used 

	# Developent
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doCreateRelativePackage functions



# eof file: doc/txt/url-sniper/create-relative-package/create-relative-package.func.txt
