# doc/txt/url-sniper/generate-docker-file/generate-docker-file.test.txt

	doTestGenerateDockerFile
	
	# Purpose:
	# ------------------------------------------------------
	to test the generate-docker-file action

	# Prequisites
	# ------------------------------------------------------
	 the docker-engine installed

	# Installation
	# ------------------------------------------------------
	 the docker-engine installed

	# Interdependancies
	# ------------------------------------------------------
	 the docker-engine installed
   a Dockerfile.tpl - template file with %var% template vars

	# Usage:
	# ------------------------------------------------------
   add the following line in your src/bash/url-sniper/tests/run-url-sniper-tests.lst
   tests list file:
   generate-docker-file
   and call the test script:
   bash src/bash/url-sniper/test-url-sniper.sh
   
	# Developent
	# ------------------------------------------------------
   - todo: how to verify programatically that the test has succeeded ?!
   
	# Caveats:
	# ------------------------------------------------------
   none known so far 


# eof file: doc/txt/url-sniper/generate-docker-file/generate-docker-file.test.txt
